# Calculate the area of a triangle

length = float(input("Length of triangle's base: "))
height = float(input("Height of triangle: "))

area = (1 / 2) * length * height

print("The area of the triangle is", area)
